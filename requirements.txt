# Voice recognition
SpeechRecognition

# Required by SpeechRecognition for microphone input.
# On Windows, install the wheel matching your Python version if pip cannot find one.
pyaudio

# Gesture recognition (default input method). Free, open-source, offline only.
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0

# HTTP control of VLC and MPC-HC web interfaces
requests

# Keyboard shortcut fallback when HTTP control is unavailable.
# Note: on Windows, global shortcuts may require running as administrator.
keyboard

# Optional native VLC bindings (not required for HTTP control)
python-vlc

# Optional: offline text-to-speech feedback (speaks commands aloud)
pyttsx3

# Optional: offline, open-source speech recognition (--recognizer vosk).
# Also downloads a small English model on first use (~50 MB) into ~/.cache/vosk
# unless the vosk_model_path config value points at an existing model.
# vosk

# Optional: offline wake-word engine (Porcupine by Picovoice). Needs a free
# Picovoice access key (config wake.porcupine_access_key or env
# PICOVOICE_ACCESS_KEY). Falls back to string-based wake words if missing.
pvporcupine

# Optional: system tray icon (--tray). Pillow is used to draw the icon.
# If missing, the app falls back to running in the terminal.
pystray
Pillow